# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Vercel
.vercel

# Env vars (nunca commitear)
.env
.env.*
!.env.example

# Supabase CLI: guarda a qué proyecto está linkeado el checkout local
supabase/.temp

# Ronda de fix 1 sobre Task 13 (revisión rigurosa, critical): `assets-privados/`
# ESTUVO acá — la lógica de "es un artefacto derivado, se regenera con
# scripts/build-pdf-recursos.mjs" no sobrevivió el chequeo empírico: nada en el
# build ejecuta ese script, así que en producción el PDF simplemente no existe
# y el UPDATE que quema el token de un solo uso corre igual, antes del intento
# de lectura — el lead pierde el PDF para siempre. 263KB es chico: se versiona.
